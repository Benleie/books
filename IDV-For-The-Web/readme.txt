08 ScatterPlot-click-toggle 
		- 自己结合click事件，让每个circle的text被点击后实现toggle效果，页面更为简洁。。
		- 每个circle都有独立的toggle下效果.  (lock)
   
   histogram-zhihu
   		- bar图标位于SVG的正中心  （lefter）
   		- CSS hover效果   (fill)
09 Axes 

   transition
   		- dataset的初始化和更新都由updateData()函数完成。
   		- 工厂模式设计多个svg对象    (selection.filter()  CreateHis()  updateSVG())








FCC d3部分 基本Bar和scatterPlot的制作